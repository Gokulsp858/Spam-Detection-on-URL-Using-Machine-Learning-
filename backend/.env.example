# Backend environment variables
# Copy to .env and adjust as needed
HOST=0.0.0.0
PORT=5000
FLASK_DEBUG=true
# Restrict CORS to your frontend origin (e.g., http://localhost:3000)
FRONTEND_ORIGIN=http://localhost:3000
# Optional: Google Safe Browsing API key
GOOGLE_API_KEY=